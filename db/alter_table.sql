-- Unused, here to show the command used to alter table adding img, mortgage, rent (were already there so dummy values)

ALTER TABLE houses
    ADD test1 VARCHAR(20),
    ADD test2 TEXT,
    ADD test3numz INTEGER;